$kind: http-request
name: 11 - Delete receipt
url: "{{base_url}}/api/receipts/{{receipt_id}}"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Estado HTTP 204\", function () {\r

      \  pm.response.to.have.status(204);\r

      });\r

      \r

      pm.test(\"La respuesta no contiene cuerpo\", function () {\r

      \  pm.expect(pm.response.text()).to.eql(\"\");\r

      });"
    language: text/javascript
order: 4503546972599680
